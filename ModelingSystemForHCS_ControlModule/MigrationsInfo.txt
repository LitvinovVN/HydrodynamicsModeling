Add-Migration Initial -StartupProject ModelingSystem.Server
Update-Database -StartupProject ModelingSystem.Server


dotnet ef migrations bundle --self-contained -r linux-x64
./efbundle


docker ps
docker ps -f "name=mysql" --format "{{.ID}}"

docker exec -ti <containerId> bash
docker exec -ti (docker ps -f "name=mysql" --format "{{.ID}}") bash

mysql -u admin -ppassword ModelingSystem < "/var/mysql-migrations/migrateModelingSystem.sql"
